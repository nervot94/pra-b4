<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FotoKiosk.GalleryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FotoKiosk"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="*, Auto" Background="#F1F5F9">
        <GridView Grid.Row="0" x:Name="ImageGridView"/>
        
        <Grid Grid.Row="1" Background="#FFDADEE3" Height="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Left Aligned Text -->
            <TextBlock Grid.Column="0" 
                       Margin="10,0" 
                       VerticalAlignment="Center"
                       Text="Aantal fotos geselecteerd: 0" />

            <!-- Right Aligned Button Panel -->
            <StackPanel Grid.Column="2" 
                        Orientation="Horizontal" 
                        Spacing="5">
                <Button Margin="10,0" 
                        VerticalAlignment="Center" 
                        Content="Bestellen" />
            </StackPanel>
        </Grid>

    </Grid>
</Page>
